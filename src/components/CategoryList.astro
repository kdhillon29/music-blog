---
import type { CollectionEntry } from "astro:content";
import Category from "./Category.astro";
type Props = {
  categories: CollectionEntry<"posts">["data"]["categories"];
  title?: string;
};

const { categories, title } = Astro.props;
---

<div class="grid grid-cols-[auto,1fr] gap-x-14 gap-y-3 my-3 max-lg:grid-cols-1">
  {
    title && (
      <h2 class="font-bold text-2xl md:text-3xl text-teal-900">{title}</h2>
    )
  }

  <div class="flex gap-4 flex-wrap items-center">
    {categories.map((category) => <Category category={category} />)}
  </div>
</div>
